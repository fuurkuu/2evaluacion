#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int lanzarDado() {
    return rand() % 6 + 1;  
}

int main() {
    srand(time(NULL));
    printf("Resultado del lanzamiento: %d\n", lanzarDado());



    return 0;
}
